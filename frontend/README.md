# Formcraft — frontend

Next.js 16 (App Router) + TypeScript frontend for the Typeform clone. See the [repository root README](../README.md) for setup, architecture, and full documentation.

Quick start:

```bash
npm install
cp .env.local.example .env.local
npm run dev
```
